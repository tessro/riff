pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.go"
      run: golangci-lint run --fix
      stage_fixed: true
    format:
      glob: "*.go"
      run: gofmt -w {staged_files}
      stage_fixed: true
